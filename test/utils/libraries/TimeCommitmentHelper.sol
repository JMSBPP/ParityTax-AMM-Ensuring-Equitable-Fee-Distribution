// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.20;

library TimeCommitmentHelperLibrary {
    function getFirst8BitsFromBytes(
        bytes memory hookData
    ) internal pure returns (uint8) {
        // It returns the first 8 bits 
        //(a.k.a most left bits) of a bytes array
    }
}
